{
  "name": "mahjong-scorekeeper",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.cjs",
    "deploy": "node build.cjs && npx wrangler pages deploy",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^2.0.0"
  }
}
